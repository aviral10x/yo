import { SectionPlaceholder } from "@/components/dashboard/section-placeholder";

export default function PortfolioPage() {
  return (
    <SectionPlaceholder
      title="Portfolio"
      description="All your venues in one view — occupancy, enquiries, and revenue across properties."
    />
  );
}
