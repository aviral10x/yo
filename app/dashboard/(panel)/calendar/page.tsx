import { SectionPlaceholder } from "@/components/dashboard/section-placeholder";

export default function CalendarPage() {
  return (
    <SectionPlaceholder
      title="Calendar"
      description="Availability across spaces and dates — holds, confirmed bookings, and blackouts."
    />
  );
}
