import { SectionPlaceholder } from "@/components/dashboard/section-placeholder";

export default function InventoryPage() {
  return (
    <SectionPlaceholder
      title="Inventory"
      description="Spaces, room blocks, and packages — what you sell and how much fits."
    />
  );
}
