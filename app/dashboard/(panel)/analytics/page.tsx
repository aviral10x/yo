import { SectionPlaceholder } from "@/components/dashboard/section-placeholder";

export default function AnalyticsPage() {
  return (
    <SectionPlaceholder
      title="Analytics"
      description="Funnel, response times, lead sources, and revenue — proof it's working."
    />
  );
}
