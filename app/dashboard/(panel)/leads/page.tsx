import { SectionPlaceholder } from "@/components/dashboard/section-placeholder";

export default function LeadsPage() {
  return (
    <SectionPlaceholder
      title="Leads"
      description="Inquiry CRM — capture, qualify, and move enquiries through the pipeline."
    />
  );
}
